/* eslint-disable react/button-has-type */
import React, { ReactNode } from "react";

function Button() {
  return (
    <div>
      {/* prev</button> */}
      <button> next</button>
    </div>
  );
}

export default Button;
